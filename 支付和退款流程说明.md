# 预约支付和退款流程说明

## 一、支付流程

### 1. 用户点击支付按钮
- 位置：`pages/appointment/detail.vue` -> `handlePay()`
- 操作：显示确认弹窗，用户确认后开始支付流程

### 2. 创建业务订单（支付前）
- 位置：`utils/payment.js` -> `createAndPayWithUniPay()`
- 操作：
  - 调用 `payment-create.create()` 云函数创建订单
  - 订单状态：`pending`
  - 返回：`order_id`, `order_no`

### 3. 发起 uni-pay 支付
- 位置：`utils/payment.js` -> `payWithUniPay()`
- 操作：
  - 使用 `order_no`（业务订单号）和 `out_trade_no`（支付单号）发起支付
  - 在 `custom` 中传递 `order_id`，供支付成功后使用

### 4. 支付成功回调
- 位置：`pages/appointment/detail.vue` -> `onPaySuccess()`
- 操作：
  - 从 `res.order_no` 或 `res.custom.order_id` 获取订单信息
  - 调用 `payment-create.mockPaySuccess()` 更新订单状态为 `paid`
  - 更新预约状态：`parent_paid = true`, `status = 'pending_confirm'` 或 `'confirmed'`
  - 保存 `out_trade_no` 到订单表，供退款时使用

### 5. 支付失败处理
- 位置：`pages/appointment/detail.vue` -> `onPayFail()`
- 操作：跳转到支付结果页面

## 二、退款流程

### 1. 用户申请退款
- 位置：`pages/appointment/detail.vue` -> `handleRefund()`
- 操作：
  - 检查是否已支付
  - 查找对应的支付订单（通过 `appointment_id`）
  - 跳转到退款申请页面

### 2. 填写退款信息
- 位置：`pages/order/refund.vue`
- 操作：
  - 选择退款类型（仅退款/退款并取消）
  - 选择退款原因
  - 填写退款说明

### 3. 提交退款申请
- 位置：`pages/order/refund.vue` -> `submitRefund()`
- 云函数：`payment-refund.apply()`
- 操作：
  - 验证订单状态（必须是 `paid` 或 `success`）
  - 创建退款记录（`payment-refunds` 表）
  - 更新订单状态为 `refunding`
  - 更新预约状态为 `refund_status: 'pending'`

### 4. 教师审核（可选）
- 位置：教师端预约详情页
- 云函数：`payment-refund.teacherReview()`
- 操作：
  - 教师同意/拒绝退款申请
  - 如果拒绝，订单状态恢复为 `paid`，预约状态为 `refund_status: 'rejected'`
  - 如果同意，退款状态变为 `processing`，等待平台审核

### 5. 平台审核
- 位置：后台管理系统 `houtai/pages/appointments/refund-review.vue`
- 云函数：`payment-refund.review()`
- 操作：
  - 查找 `out_trade_no`（从 `payment-orders` 或 `uni-pay-orders`）
  - 调用 `uni-pay-co.refund()` 执行实际退款
  - 更新订单状态为 `refunded`
  - 更新预约状态为 `refund_status: 'success'` 或 `'rejected'`

## 三、关键数据表

### payment-orders（支付订单表）
- `order_no`: 业务订单号
- `out_trade_no`: uni-pay 支付单号（用于退款）
- `appointment_id`: 关联预约ID
- `status`: 订单状态（pending -> paid -> refunding -> refunded）
- `payer_id`: 支付人ID

### payment-refunds（退款申请表）
- `order_id`: 关联的支付订单ID
- `appointment_id`: 关联预约ID
- `status`: 退款状态（pending -> processing -> success/rejected）
- `teacher_review_status`: 教师审核状态
- `amount`: 退款金额

### appointments（预约表）
- `parent_paid`: 家长是否已支付
- `refund_status`: 退款状态
- `status`: 预约状态

## 四、修复的问题

### 1. 订单创建时机
- **问题**：之前是在支付成功后创建订单，导致找不到订单
- **修复**：改为在支付前创建订单，支付成功后只更新状态

### 2. 支付流程混乱
- **问题**：开发模式没有真正创建订单
- **修复**：统一使用云函数创建订单，不再区分开发/生产模式

### 3. 退款找不到订单
- **问题**：订单创建时机不对，导致退款时找不到
- **修复**：确保订单在支付前创建，退款时能正确找到

### 4. out_trade_no 保存
- **问题**：退款时需要 `out_trade_no`，但没有保存
- **修复**：支付成功后，将 `out_trade_no` 保存到 `payment-orders` 表

## 五、注意事项

1. **订单创建时机**：必须在支付前创建，不能等到支付成功后再创建
2. **订单状态流转**：`pending` -> `paid` -> `refunding` -> `refunded`
3. **退款流程**：需要教师审核（可选）-> 平台审核 -> 执行退款
4. **数据一致性**：确保订单、预约、退款三个表的状态保持一致
