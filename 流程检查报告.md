# 小程序流程检查报告

## 一、当前实现情况

### ✅ 已实现的流程

1. **联系老师流程**
   - ✅ 家长可以联系老师（`contactTeacher` 方法）
   - ✅ 联系请求创建会话（`createContactRequest`）
   - ✅ 会话创建时 `chat_enabled: false`，需老师支付保证金后开启

2. **保证金支付流程**
   - ✅ 老师支付保证金后才能和家长聊天
   - ✅ 支付保证金后更新 `chat_enabled: true`
   - ✅ 保证金检查逻辑（`payment-create` 中检查是否已支付）

3. **试课邀请流程**
   - ✅ 老师可以邀请家长试课（`inviteTrial`）
   - ✅ 家长接受试课邀请并填写信息
   - ✅ 家长支付试课费用
   - ✅ 老师确认预约

4. **试课完成流程**
   - ✅ 试课完成后家长确认订单（`confirmTrialSuccess`）
   - ✅ 试课成功后可以评价老师（`createReview`）

5. **正式课程预约**
   - ✅ 联系过老师后可以预约正式课程
   - ✅ 试课预约只能由老师发起邀请

## 二、需要修复的问题

### ❌ 问题1：家长首次联系老师时未自动发送孩子信息

**当前状态**：
- `contactTeacher` 方法只是跳转到聊天页面，没有自动发送孩子信息
- 聊天页面没有首次联系时自动发送信息的逻辑

**需要实现**：
- 首次联系时，自动发送包含孩子信息的消息
- 消息格式：学生姓名、年级、科目等信息

**建议修改**：
1. 在 `pages/chat/conversation.vue` 中，检测到 `isFirstContact=true` 时，自动发送孩子信息
2. 或者修改 `contactTeacher` 方法，在跳转前先发送一条包含孩子信息的消息

---

### ❌ 问题2：联系老师前未验证是否完善信息

**当前状态**：
- 没有在联系老师前检查家长是否完善了孩子信息
- 家长可以直接联系老师，即使没有填写孩子信息

**需要实现**：
- 联系老师前检查：`student_name`、`student_grade`、`subject` 是否已填写
- 如果未完善，提示家长先完善信息

**建议修改**：
1. 在 `pages/appointment/detail.vue` 的 `contactTeacher` 方法中，添加信息验证
2. 检查 `userInfo.parent_info` 或从数据库查询家长信息
3. 如果信息不完整，跳转到信息完善页面或显示提示

---

### ❌ 问题3：保证金应该无法退，但当前代码会退还

**当前状态**：
- `appointment-complete/index.obj.js` 的 `confirmTrialSuccess` 方法中：
  ```javascript
  // 8. 更新教师钱包（收入 + 保证金退还）
  await updateTeacherWallet(appointment.teacher_id, teacherIncome, depositAmount)
  ```
- 保证金会被退还给老师

**需要实现**：
- 保证金无法退，作为平台费用或违约金
- 试课完成后，保证金不退还给老师

**建议修改**：
1. 修改 `confirmTrialSuccess` 方法，移除保证金退还逻辑
2. 或者将保证金作为平台费用，不加入教师钱包

---

### ❌ 问题4：试课完成后家长确认订单的入口不明确

**当前状态**：
- 有 `confirmTrialSuccess` 云函数，但前端可能没有调用入口
- 需要检查 `pages/appointment/detail.vue` 是否有"确认试课成功"按钮

**需要实现**：
- 试课完成后，在家长端显示"确认试课成功"按钮
- 点击后调用 `appointment-complete.confirmTrialSuccess`
- 确认成功后显示评价按钮

**建议检查**：
- `pages/appointment/detail.vue` 中是否有 `status === 'in_progress'` 或 `status === 'confirmed'` 时的确认按钮

---

### ❌ 问题5：后续预约正式课程或直接交易的逻辑不明确

**当前状态**：
- 试课完成后，可以继续预约正式课程（已有逻辑）
- 但"直接交易"的逻辑不明确

**需要实现**：
- 明确"直接交易"的含义：
  - 是指老师和家长私下交易（不通过平台）？
  - 还是指跳过试课，直接预约正式课程？
- 如果是指私下交易，需要添加相应的功能或说明

---

## 三、建议的修复优先级

1. **高优先级**：
   - 修复保证金无法退的逻辑（问题3）
   - 添加联系老师前信息验证（问题2）
   - 添加首次联系时自动发送孩子信息（问题1）

2. **中优先级**：
   - 检查并完善试课完成后确认订单的入口（问题4）

3. **低优先级**：
   - 明确"直接交易"的含义和实现方式（问题5）

---

## 四、代码位置参考

### 关键文件
- `test/pages/appointment/detail.vue` - 家长端预约详情
- `test/pages/chat/conversation.vue` - 聊天页面
- `test/uniCloud-aliyun/cloudfunctions/appointment-create/index.obj.js` - 预约创建
- `test/uniCloud-aliyun/cloudfunctions/appointment-complete/index.obj.js` - 订单完成
- `test/uniCloud-aliyun/cloudfunctions/payment-create/index.obj.js` - 支付创建
- `test/uniCloud-aliyun/cloudfunctions/chat-send/index.obj.js` - 聊天发送
